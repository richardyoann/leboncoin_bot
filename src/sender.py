
class MessageSender:
    def send(self, annonce, message):
        # Simulation d’envoi (à remplacer par vrai envoi si besoin)
        print(f"[Envoi] Message à {annonce['title']}:")
        print(message)
        print("-" * 40)